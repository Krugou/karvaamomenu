{
  "name": "web-menu-scraper",
  "version": "1.0.0",
  "description": "A simple web scraper to get the menu from a restaurant website",
  "main": "menu.js",
  "scripts": {
    "start": "node menu.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon menu.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}